#include "main.h"
/**
 * alloc_grid - create a pointer tp 2d array
 * width: width of array
 * height: height od array
 *
 * Return: pointer to the array
 */

int **alloc_grid(int width, int height)
{
	if (width <= 0 | width == NULL | height <= 0 | height == NULL)
		return (NULL);
}
